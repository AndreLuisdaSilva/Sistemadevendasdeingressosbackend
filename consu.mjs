import {area, circus as Circue} from "./define.mjs";

console.log(`A areá do circulo: ${area(6)}`)

console.log(`A areá do circulo 1: ${Circue(6)}`)

import * as Circos from "./define.mjs";

console.log(`A areá do circulo 2: ${Circos.area(6)}`)